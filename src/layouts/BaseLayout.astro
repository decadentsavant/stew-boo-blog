---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import RadialGlow from "../components/RadialGlow.astro";
import { navLinks } from "../components/Navigation.astro";
const { pageTitle, ogUrl, ogType, ogDescription, ogImage, ogImageAlt } = Astro.props;
// Determine the active link text by matching the current URL path
let navTitle;
for (let link of navLinks) {
    if (Astro.url.pathname === link.url) {
        navTitle = link.text;
        break;
    }
}
---

<html lang="en" class="bg-main-bg-color text-text-whiteish">
    <head>
        <meta charset="utf-8" />
        <link rel="icon" type="image/x-icon" href="/images/favicon.ico" />
        <meta name="viewport" content="width=device-width" />
        <meta name="generator" content={Astro.generator} />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Anton&family=Inconsolata:wght@400;700&family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap"
            rel="stylesheet"
        />
        <title>{pageTitle}</title>
        <!-- Open Graph Meta Tags -->
        <meta property="og:url" content="https://stew.boo/" />
        <meta property="og:type" content="website" />
        <meta property="og:title" content="Welcome to Stew.Boo!" />
        <meta
            property="og:description"
            content="Your brief description of the content goes here."
        />
        <meta
            property="og:image"
            content="https://stew.boo/images/madeira.jpg"
        />
        <meta
            property="og:image:alt"
            content="A descriptive text about the image."
        />
        <meta property="og:updated_time" content="1695845423" />
    </head>
    <body>
        <div
            class="mx-auto p-4 w-full flex flex-col min-h-screen lg:max-w-screen-lg"
        >
            <Header navTitle={navTitle} />
            <div class="flex-grow">
                <slot />
            </div>
            <Footer />
        </div>
    </body>
    <RadialGlow />
</html>
