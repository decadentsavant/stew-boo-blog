---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { navLinks } from "../components/Navigation.astro"; // Import navLinks
const { pageTitle } = Astro.props;
// Determine the active link text by matching the current URL path
let navTitle;
for (let link of navLinks) {
    if (Astro.url.pathname === link.url) {
        navTitle = link.text;
        break;
    }
}
---

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <link rel="icon" type="image/x-icon" href="/images/favicon.ico" />
        <meta name="viewport" content="width=device-width" />
        <meta name="generator" content={Astro.generator} />
        <title>{pageTitle}</title>
    </head>
    <body>
        <div class="mx-auto p-4 w-full flex flex-col min-h-screen lg:max-w-screen-lg">
            <Header navTitle={navTitle}/>
            <div class="flex-grow">
            <h1 class="text-xl font-bold"></h1>
            <slot />
            </div>
            <Footer />
        </div>
    </body>
</html>
