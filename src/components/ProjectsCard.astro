---
const { img, title, desc, alt, shortDesc, project } = Astro.props;
---

<div
    class="project-card-class flex flex-col max-w-lg rounded-lg bg-neutral-800 cursor-pointer"
    data-stringified-project={project}
>
    <img class="object-cover h-72" src={img} alt={alt} />
    <div class="flex flex-col justify-start p-6 bg-neutral-700 rounded-b-lg">
        <h2 class="mb-2 text-xl font-medium text-neutral-50">
            {title}
        </h2>
        <p class="mb-4 text-base text-neutral-50">
            {shortDesc}
        </p>
        <p class="text-xs text-neutral-500">Click to learn more</p>
    </div>
</div>
